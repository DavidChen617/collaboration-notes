# 只宣告這個 Secret 的存在與 key 名稱，不放真的 token——真實內容由伺服器端
# `infra/scripts/apply-secrets.sh` 寫入。見 infra/k8s/postgres/secret.yaml 開頭的
# 註解，理由跟這裡的 ignoreDifferences 設定完全一樣。
apiVersion: v1
kind: Secret
metadata:
  name: cloudflared-credentials
  namespace: collaboration-notes
type: Opaque
stringData:
  TUNNEL_TOKEN: ""
