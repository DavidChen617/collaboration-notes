# Toy/learning project: placeholder credentials committed directly, matching
# the dev-time placeholders already committed in src/CoNotes.Api/appsettings.json.
# Replace before any real-world use.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: collaboration-notes
type: Opaque
stringData:
  postgres-password: changeme-postgres-superuser
  app-password: changeme-app
  keycloak-password: changeme-keycloak
