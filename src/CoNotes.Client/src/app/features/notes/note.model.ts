export interface NoteSummary {
  noteId: string;
  title: string;
  content: string;
  createdAt: string;
  updatedAt: string;
}

export interface NoteDetail {
  noteId: string;
  title: string;
  content: string;
  createdAt: string;
  updatedAt: string;
}
